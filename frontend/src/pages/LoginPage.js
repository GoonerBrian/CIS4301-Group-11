const LoginPage = () => {
    return (
        <h1>This is the login page. Ideally, only either the My Profile or Login page will be present at a time. Meaning, once a user logs in they will see the My Profile option instead of Login.</h1>
    );
}

export default LoginPage;