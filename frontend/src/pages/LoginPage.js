const LoginPage = () => {
    return (
        <>
        <h1>Login Page Stub</h1>
        <p>This is the login page. Ideally, only either the My Profile or Login page will be present at a time. Meaning, once a user logs in they will see the My Profile option instead of Login.</p>
        </>
    );
}

export default LoginPage;