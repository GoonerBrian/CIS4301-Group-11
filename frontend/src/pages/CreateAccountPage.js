const CreatAccountPage = () => {
    return (
        <>
        <h1>Creat Account</h1>
        <p>Probably want to just include a button from the login page to here. Doesn't need to be linked to on other pages.</p>
        </>
    );
}

export default CreatAccountPage;