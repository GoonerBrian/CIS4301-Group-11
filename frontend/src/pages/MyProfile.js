const MyProfile = () => {
    return (
        <h1>This is the my profile page. We can use this to store the user's saved queries. Ideally, only either the My Profile or Login page will be present at a time. Meaning, once a user logs in they will see the My Profile option instead of Login.</h1>
    );
}

export default MyProfile;